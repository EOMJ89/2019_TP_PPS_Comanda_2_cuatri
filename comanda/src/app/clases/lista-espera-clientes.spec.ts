import { ListaEsperaClientes } from './lista-espera-clientes';

describe('ListaEsperaClientes', () => {
  it('should create an instance', () => {
    expect(new ListaEsperaClientes()).toBeTruthy();
  });
});
